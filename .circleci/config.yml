version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/node:4.8.2-browsers
    steps:
      - checkout
      - node/with-cache:
        steps:
          - run: npm install
          - run: npm test
